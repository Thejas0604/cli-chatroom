# CLI Chatroom

A simple command-line interface (CLI) chatroom application.

## Features

- Real-time messaging

## Usage

1. Start the server:
    ```sh
    py server.py
    ```
2. Open a new terminal and run the client:
    ```sh
    py client.py
    ```


